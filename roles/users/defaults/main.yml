users_partitions: >-
  {{
      ['cpu']
      +
      ('gpu' in item.slurm_groups | ternary(['gpu'],[]))
      +
      ('highmem' in item.slurm_groups | ternary(['highmem'],[]))
  }}

ldap_user_auth_type:
  - otp
